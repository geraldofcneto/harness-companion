<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="top|center_horizontal"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".ExecTurnList" >

    <AutoCompleteTextView
        android:id="@+id/execTurnNameAutoCompleteView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:ems="10"
        android:completionThreshold="1"
        android:hint="@string/execution_turn_name" >

        <requestFocus />
    </AutoCompleteTextView>

    <TextView
        android:id="@+id/executadosTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:visibility="invisible"
        android:text="@string/executados" />

    <ProgressBar
        android:id="@+id/execTurnProgressBar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        android:progressDrawable="@drawable/harness_progress_style" />

    <TextView
        android:id="@+id/vermelhosVerdesTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:visibility="invisible"
        android:text="@string/vermelhos_verdes" />

    <ProgressBar
        android:id="@+id/redGreenProgressBar"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:visibility="invisible"
        android:progressDrawable="@drawable/red_green_harness_progress_style" />

</LinearLayout>